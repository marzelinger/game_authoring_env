package View;

//import javafx.scene.Scene;
//import javafx.scene.text.Text;

public interface InternalViewAPI {

    /**
     * Method to update the graphical position of the turtle
     */
    //public void updateLoop(TurtleView turtleView, TurtleStatus newStatus, boolean turtleState, double xOffset, double yOffset, Map<String, List<Node>> functions, Map<String, List<Node>> variables);

    /**
     * Method to generate a correctly typed Help object
     */
    //public Help createHelp(String helpType, UserHistory userHistory);

    /**
     * Method to initialize the GUI
     */
    //public Scene initializeScene(double width, double height);

    /**
     * Add to the command history
     */
  //  public void updateUserHistory(Text newInstruction);

  //  public void changeLanguage(Text langText) {
   //     langText.setText("Language: " + currLanguage);
   // }

   // public void changeColor(Text colorText)
   // {
   //     colorText.setText("Pen color: " + penColor);
   // }
}
